/****************************************************************************
 * Exports for Closure Compiler <http://code.google.com/closure/compiler/>.
 * Only symbols referenced directly from HTML need to be exported.
 ****************************************************************************/

// main.js
window['JSOTP_FORMAT'] = JSOTP_FORMAT;
window['JSOTP_PW_WINDOW'] = JSOTP_PW_WINDOW;
window['with_hash'] = with_hash;

// otp.js
window['a_to_both'] = a_to_both;
window['a_to_hex'] = a_to_hex;
window['a_to_6word'] = a_to_6word;
